name: uas_komnas_blog
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"
dependencies:
  cupertino_icons: ^1.0.2
  equatable: ^2.0.3
  firebase_core: ^1.11.0
  flutter:
    sdk: flutter
  flutter_svg: ^1.0.0
  fluttertoast: ^8.0.8
  get: ^4.6.1
  google_fonts: ^2.2.0
  intl: ^0.17.0
  ionicons: ^0.1.2
  shared_preferences: ^2.0.12
  syncfusion_flutter_pdfviewer: ^19.4.41-beta
  url_launcher: ^6.0.18
  video_player: ^2.2.11
  video_player_web: ^2.0.6
  youtube_player_iframe: ^2.2.2

dev_dependencies:
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/icons/
    - assets/pdf/
